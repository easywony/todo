# TodoList 화면 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TCA 기반 할 일 목록 화면 구현 — Todo 모델, Repository, Reducer, SwiftUI View를 포함한 완전한 첫 번째 기능

**Architecture:** TCA 1.25.5, NavigationStack + List(.insetGrouped), Repository 패턴으로 UserDefaults에 JSON 저장. 완료 토글·삭제는 낙관적 업데이트(State 먼저 변경 후 Effect로 저장).

**Tech Stack:** Swift 5.9+, SwiftUI, TCA 1.25.5 (swift-composable-architecture), UserDefaults

---

## 파일 구조

| 파일 | 작업 | 역할 |
|------|------|------|
| `todo/Models/Todo.swift` | 생성 | 데이터 모델 (Identifiable, Equatable, Codable) |
| `todo/Dependencies/TodoRepository.swift` | 생성 | Repository 프로토콜 + liveValue / testValue |
| `todo/Resources/DesignTokens.swift` | 생성 | 우선순위 색상 토큰, Priority 표시 레이블 |
| `todo/Features/TodoList/TodoListFeature.swift` | 생성 | TCA Reducer (State, Action, Effect) |
| `todo/Features/TodoList/TodoListView.swift` | 생성 | SwiftUI View + TodoRowView |
| `todo/todoApp.swift` | 수정 | TodoListView를 루트 화면으로 연결 |
| `todoTests/TodoListFeatureTests.swift` | 생성 | Reducer 단위 테스트 (TCA TestStore) |
| `todo.xcodeproj/project.pbxproj` | 수정 | 새 파일 타겟 등록 |

---

### Task 1: 브랜치 및 디렉토리 생성

- [ ] **Step 1: feature 브랜치 생성**

```bash
git checkout -b feature/todo-list
```

Expected: `Switched to a new branch 'feature/todo-list'`

- [ ] **Step 2: 소스 디렉토리 생성**

```bash
mkdir -p todo/Models todo/Dependencies todo/Features/TodoList todo/Resources
```

Expected: no output (success)

---

### Task 2: Todo 모델 생성

**Files:**
- Create: `todo/Models/Todo.swift`

- [ ] **Step 1: Todo 모델 작성**

`todo/Models/Todo.swift`:

```swift
import Foundation

struct Todo: Identifiable, Equatable, Codable {
    let id: UUID
    var title: String
    var isCompleted: Bool
    var dueDate: Date?
    var priority: Priority

    enum Priority: String, CaseIterable, Equatable, Codable {
        case high, medium, low
    }

    init(
        id: UUID = UUID(),
        title: String,
        isCompleted: Bool = false,
        dueDate: Date? = nil,
        priority: Priority = .medium
    ) {
        self.id = id
        self.title = title
        self.isCompleted = isCompleted
        self.dueDate = dueDate
        self.priority = priority
    }
}
```

- [ ] **Step 2: 커밋**

```bash
git add todo/Models/Todo.swift
git commit -m "feat: Todo 데이터 모델 추가"
```

---

### Task 3: DesignTokens 생성

**Files:**
- Create: `todo/Resources/DesignTokens.swift`

- [ ] **Step 1: DesignTokens 작성**

`todo/Resources/DesignTokens.swift`:

```swift
import SwiftUI

extension Color {
    static let priorityHigh   = Color(red: 1.0,   green: 0.231, blue: 0.188) // #ff3b30
    static let priorityMedium = Color(red: 1.0,   green: 0.584, blue: 0.0)   // #ff9500
    static let priorityLow    = Color(red: 0.204, green: 0.780, blue: 0.349) // #34c759
}

extension Todo.Priority {
    var color: Color {
        switch self {
        case .high:   return .priorityHigh
        case .medium: return .priorityMedium
        case .low:    return .priorityLow
        }
    }

    var label: String {
        switch self {
        case .high:   return "높음"
        case .medium: return "중간"
        case .low:    return "낮음"
        }
    }
}
```

- [ ] **Step 2: 커밋**

```bash
git add todo/Resources/DesignTokens.swift
git commit -m "feat: 우선순위 디자인 토큰 추가"
```

---

### Task 4: TodoRepository Dependency 생성

**Files:**
- Create: `todo/Dependencies/TodoRepository.swift`

- [ ] **Step 1: TodoRepository 작성**

`todo/Dependencies/TodoRepository.swift`:

```swift
import ComposableArchitecture
import Foundation

struct TodoRepository {
    var fetchAll: @Sendable () async throws -> [Todo]
    var save: @Sendable ([Todo]) async throws -> Void
}

extension TodoRepository: DependencyKey {
    static let liveValue = TodoRepository(
        fetchAll: {
            guard
                let data = UserDefaults.standard.data(forKey: "todos"),
                let todos = try? JSONDecoder().decode([Todo].self, from: data)
            else { return [] }
            return todos
        },
        save: { todos in
            let data = try JSONEncoder().encode(todos)
            UserDefaults.standard.set(data, forKey: "todos")
        }
    )

    static let testValue = TodoRepository(
        fetchAll: { [] },
        save: { _ in }
    )
}

extension DependencyValues {
    var todoRepository: TodoRepository {
        get { self[TodoRepository.self] }
        set { self[TodoRepository.self] = newValue }
    }
}
```

- [ ] **Step 2: 커밋**

```bash
git add todo/Dependencies/TodoRepository.swift
git commit -m "feat: TodoRepository dependency 추가"
```

---

### Task 5: TodoListFeature 테스트 작성 (실패 먼저)

**Files:**
- Create: `todoTests/TodoListFeatureTests.swift`

> Note: `todoTests` 타겟이 없으면 Xcode에서 **File > New > Target > Unit Testing Bundle** 로 `todoTests` 타겟을 추가한다. 타겟 추가 후 ComposableArchitecture를 `todoTests` 타겟의 **Frameworks and Libraries** 에도 추가한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`todoTests/TodoListFeatureTests.swift`:

```swift
import ComposableArchitecture
import XCTest
@testable import todo

@MainActor
final class TodoListFeatureTests: XCTestCase {

    func test_onAppear_loadsTodos() async {
        let todo = Todo(id: UUID(), title: "테스트 할 일", priority: .high)
        let store = TestStore(initialState: TodoListFeature.State()) {
            TodoListFeature()
        } withDependencies: {
            $0.todoRepository.fetchAll = { [todo] }
        }

        await store.send(.onAppear) {
            $0.isLoading = true
        }
        await store.receive(\.todosLoaded) {
            $0.isLoading = false
            $0.todos = IdentifiedArray(uniqueElements: [todo])
        }
    }

    func test_toggleCompleted_flipsIsCompleted() async {
        let todo = Todo(id: UUID(), title: "토글 테스트", priority: .medium)
        let store = TestStore(
            initialState: TodoListFeature.State(
                todos: IdentifiedArray(uniqueElements: [todo])
            )
        ) {
            TodoListFeature()
        } withDependencies: {
            $0.todoRepository.save = { _ in }
        }

        await store.send(.toggleCompleted(id: todo.id)) {
            $0.todos[id: todo.id]?.isCompleted = true
        }
    }

    func test_deleteTodo_removesFromState() async {
        let todo = Todo(id: UUID(), title: "삭제 대상", priority: .low)
        let store = TestStore(
            initialState: TodoListFeature.State(
                todos: IdentifiedArray(uniqueElements: [todo])
            )
        ) {
            TodoListFeature()
        } withDependencies: {
            $0.todoRepository.save = { _ in }
        }

        await store.send(.deleteTodo(id: todo.id)) {
            $0.todos.remove(id: todo.id)
        }
    }
}
```

- [ ] **Step 2: 테스트 실행 (빌드 에러 확인)**

```bash
xcodebuild test \
  -scheme todo \
  -destination 'platform=iOS Simulator,name=iPhone 16' \
  -only-testing:todoTests/TodoListFeatureTests \
  2>&1 | grep -E "(error:|BUILD FAILED|BUILD SUCCEEDED)"
```

Expected: `TodoListFeature` 미정의로 빌드 에러

---

### Task 6: TodoListFeature Reducer 구현 (테스트 통과)

**Files:**
- Create: `todo/Features/TodoList/TodoListFeature.swift`

- [ ] **Step 1: Reducer 구현**

`todo/Features/TodoList/TodoListFeature.swift`:

```swift
import ComposableArchitecture

@Reducer
struct TodoListFeature {
    @ObservableState
    struct State: Equatable {
        var todos: IdentifiedArrayOf<Todo> = []
        var isLoading = false
    }

    enum Action {
        case onAppear
        case todosLoaded(Result<[Todo], Error>)
        case toggleCompleted(id: Todo.ID)
        case deleteTodo(id: Todo.ID)
        case addTodoButtonTapped
        case todoRowTapped(id: Todo.ID)
    }

    @Dependency(\.todoRepository) var todoRepository

    var body: some ReducerOf<Self> {
        Reduce { state, action in
            switch action {
            case .onAppear:
                state.isLoading = true
                return .run { send in
                    await send(.todosLoaded(
                        Result { try await todoRepository.fetchAll() }
                    ))
                }

            case .todosLoaded(.success(let todos)):
                state.isLoading = false
                state.todos = IdentifiedArray(uniqueElements: todos)
                return .none

            case .todosLoaded(.failure):
                state.isLoading = false
                return .none

            case .toggleCompleted(let id):
                state.todos[id: id]?.isCompleted.toggle()
                return .run { [todos = state.todos] _ in
                    try await todoRepository.save(Array(todos))
                }

            case .deleteTodo(let id):
                state.todos.remove(id: id)
                return .run { [todos = state.todos] _ in
                    try await todoRepository.save(Array(todos))
                }

            case .addTodoButtonTapped, .todoRowTapped:
                return .none
            }
        }
    }
}
```

- [ ] **Step 2: 테스트 실행 (통과 확인)**

```bash
xcodebuild test \
  -scheme todo \
  -destination 'platform=iOS Simulator,name=iPhone 16' \
  -only-testing:todoTests/TodoListFeatureTests \
  2>&1 | grep -E "(passed|FAILED|error:)"
```

Expected: `Test Suite 'TodoListFeatureTests' passed`

- [ ] **Step 3: 커밋**

```bash
git add todo/Features/TodoList/TodoListFeature.swift todoTests/TodoListFeatureTests.swift
git commit -m "feat: TodoListFeature Reducer 구현 및 테스트 추가"
```

---

### Task 7: TodoListView 구현

**Files:**
- Create: `todo/Features/TodoList/TodoListView.swift`

- [ ] **Step 1: View 구현**

`todo/Features/TodoList/TodoListView.swift`:

```swift
import ComposableArchitecture
import SwiftUI

struct TodoListView: View {
    @Bindable var store: StoreOf<TodoListFeature>

    var body: some View {
        NavigationStack {
            List {
                ForEach(store.todos) { todo in
                    TodoRowView(todo: todo) {
                        store.send(.toggleCompleted(id: todo.id))
                    }
                    .contentShape(Rectangle())
                    .onTapGesture {
                        store.send(.todoRowTapped(id: todo.id))
                    }
                    .swipeActions(edge: .trailing, allowsFullSwipe: true) {
                        Button(role: .destructive) {
                            store.send(.deleteTodo(id: todo.id))
                        } label: {
                            Label("삭제", systemImage: "trash")
                        }
                    }
                }
            }
            .listStyle(.insetGrouped)
            .navigationTitle("할 일")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button {
                        store.send(.addTodoButtonTapped)
                    } label: {
                        Image(systemName: "plus")
                    }
                }
            }
            .onAppear { store.send(.onAppear) }
        }
    }
}

struct TodoRowView: View {
    let todo: Todo
    let onToggle: () -> Void

    var body: some View {
        HStack(spacing: 12) {
            Button(action: onToggle) {
                Image(systemName: todo.isCompleted ? "checkmark.circle.fill" : "circle")
                    .font(.title2)
                    .foregroundStyle(todo.isCompleted ? Color.priorityLow : Color(.systemGray3))
            }
            .buttonStyle(.plain)

            VStack(alignment: .leading, spacing: 3) {
                Text(todo.title)
                    .font(.body)
                    .strikethrough(todo.isCompleted)
                    .foregroundStyle(todo.isCompleted ? .secondary : .primary)

                HStack(spacing: 6) {
                    HStack(spacing: 3) {
                        Circle()
                            .fill(todo.priority.color)
                            .frame(width: 8, height: 8)
                        Text(todo.priority.label)
                            .font(.caption)
                            .fontWeight(.medium)
                            .foregroundStyle(todo.priority.color)
                    }

                    if let date = todo.dueDate {
                        Text(date.formatted(.dateTime.month().day()))
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                }
            }
        }
        .opacity(todo.isCompleted ? 0.45 : 1.0)
    }
}

#Preview {
    TodoListView(
        store: Store(initialState: TodoListFeature.State(
            todos: IdentifiedArray(uniqueElements: [
                Todo(title: "디자인 시스템 토큰 정리", dueDate: Date(), priority: .high),
                Todo(title: "TodoList Feature 구현", priority: .medium),
                Todo(title: "README 업데이트", priority: .low),
                Todo(title: "초기 프로젝트 설정", isCompleted: true, priority: .medium),
            ])
        )) {
            TodoListFeature()
        }
    )
}
```

- [ ] **Step 2: 커밋**

```bash
git add todo/Features/TodoList/TodoListView.swift
git commit -m "feat: TodoListView SwiftUI 구현"
```

---

### Task 8: App 진입점 연결

**Files:**
- Modify: `todo/todoApp.swift`

- [ ] **Step 1: todoApp.swift 수정**

`todo/todoApp.swift` 전체를 아래로 교체:

```swift
import ComposableArchitecture
import SwiftUI

@main
struct todoApp: App {
    var body: some Scene {
        WindowGroup {
            TodoListView(
                store: Store(initialState: TodoListFeature.State()) {
                    TodoListFeature()
                }
            )
        }
    }
}
```

- [ ] **Step 2: ContentView.swift 삭제**

```bash
rm todo/ContentView.swift
```

- [ ] **Step 3: 커밋**

```bash
git add todo/todoApp.swift
git commit -m "feat: TodoListView를 앱 루트 화면으로 연결"
```

---

### Task 9: Xcode 프로젝트에 새 파일 등록

**Files:**
- Modify: `todo.xcodeproj/project.pbxproj`

- [ ] **Step 1: xcodeproj gem 설치**

```bash
gem install xcodeproj 2>&1 | tail -2
```

Expected: `1 gem installed` 또는 `already installed`

- [ ] **Step 2: 파일 등록 스크립트 실행**

`add_files.rb` 파일을 작성:

```ruby
require 'xcodeproj'

project = Xcodeproj::Project.open('todo.xcodeproj')
app_target = project.targets.find { |t| t.name == 'todo' }
todo_group = project.main_group['todo']

def get_or_create_group(parent, name)
  parent[name] || parent.new_group(name, name)
end

{
  'Models'            => ['Todo.swift'],
  'Dependencies'      => ['TodoRepository.swift'],
  'Resources'         => ['DesignTokens.swift'],
}.each do |folder, files|
  group = get_or_create_group(todo_group, folder)
  files.each do |filename|
    next if group.files.any? { |f| f.path == filename }
    ref = group.new_file(filename)
    app_target.source_build_phase.add_file_reference(ref)
  end
end

# Features/TodoList (중첩 그룹)
features_group   = get_or_create_group(todo_group, 'Features')
todolist_group   = get_or_create_group(features_group, 'TodoList')
['TodoListFeature.swift', 'TodoListView.swift'].each do |filename|
  next if todolist_group.files.any? { |f| f.path == filename }
  ref = todolist_group.new_file(filename)
  app_target.source_build_phase.add_file_reference(ref)
end

# ContentView.swift 제거 (삭제한 파일)
todo_group.files.select { |f| f.path == 'ContentView.swift' }.each do |ref|
  app_target.source_build_phase.remove_file_reference(ref)
  ref.remove_from_project
end

project.save
puts 'Done.'
```

```bash
ruby add_files.rb && rm add_files.rb
```

Expected: `Done.`

- [ ] **Step 3: 커밋**

```bash
git add todo.xcodeproj/project.pbxproj
git commit -m "chore: 새 소스 파일 Xcode 프로젝트에 등록"
```

---

### Task 10: 빌드 및 테스트 검증

- [ ] **Step 1: 전체 빌드**

```bash
xcodebuild build \
  -scheme todo \
  -destination 'platform=iOS Simulator,name=iPhone 16' \
  2>&1 | grep -E "(BUILD SUCCEEDED|BUILD FAILED|error:)"
```

Expected: `BUILD SUCCEEDED`

- [ ] **Step 2: 전체 테스트 실행**

```bash
xcodebuild test \
  -scheme todo \
  -destination 'platform=iOS Simulator,name=iPhone 16' \
  2>&1 | grep -E "(passed|FAILED|error:)"
```

Expected: `Test Suite 'All tests' passed`

- [ ] **Step 3: 커밋 이력 확인**

```bash
git log --oneline feature/todo-list ^main
```

Expected 커밋 목록 (최신 → 오래된 순):
```
chore: 새 소스 파일 Xcode 프로젝트에 등록
feat: TodoListView를 앱 루트 화면으로 연결
feat: TodoListView SwiftUI 구현
feat: TodoListFeature Reducer 구현 및 테스트 추가
feat: TodoRepository dependency 추가
feat: 우선순위 디자인 토큰 추가
feat: Todo 데이터 모델 추가
```
